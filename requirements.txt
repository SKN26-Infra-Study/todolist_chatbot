# Discord Todo 봇
discord.py>=2.3.0
python-dotenv>=1.0.0
supabase>=2.0.0
httpx>=0.27.0

# LLM 클라이언트는 사용 API에 맞게 선택 (예: openai)
# openai>=1.0.0

# 개발
pytest>=8.0.0

# 선택: 노트북·레거시 RAG 실험 시 legacy/rag_stack 참고용 의존성은 별도 requirements-legacy.txt 권장
